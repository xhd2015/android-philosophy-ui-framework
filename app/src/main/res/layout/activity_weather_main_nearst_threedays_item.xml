<?xml version="1.0" encoding="utf-8"?>
<android.support.constraint.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:layout_editor_absoluteY="25dp"
    tools:layout_editor_absoluteX="0dp">

    <TextView
        android:id="@+id/monthDateView"
        android:layout_width="48dp"
        android:layout_height="20dp"
        android:text="7.20"
        android:layout_marginLeft="6dp"
        app:layout_constraintLeft_toLeftOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        android:layout_marginTop="7dp"
        android:textAlignment="center"
        android:textColor="@color/white"
        />

    <TextView
        android:id="@+id/maxMinTemperature"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="0dp"
        android:layout_marginTop="8dp"
        android:text="@string/cannotAccess"
        android:textAlignment="center"
        android:textColor="@color/white"
        app:layout_constraintLeft_toLeftOf="@+id/monthDateView"
        app:layout_constraintTop_toBottomOf="@+id/weatherIndicator" />

    <ImageView
        android:layout_width="50dp"
        android:layout_height="50dp"
        android:layout_marginLeft="0dp"
        android:id="@+id/weatherIndicator"
        android:layout_marginTop="0dp"
        android:src="@drawable/rainy_vector"
        app:layout_constraintLeft_toLeftOf="@+id/monthDateView"
        app:layout_constraintTop_toBottomOf="@+id/monthDateView" />
</android.support.constraint.ConstraintLayout>